
/**
 * Re-export from the contexts folder for backward compatibility
 * This allows existing components to keep working while we transition to the new structure
 */
export * from './contexts/auth-context';
