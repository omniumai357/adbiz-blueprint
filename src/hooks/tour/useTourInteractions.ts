
import { useTourInteractions as useTourInteractionsImpl } from "./controller/interactions/use-tour-interactions";

/**
 * Re-export the tour interactions hook for backward compatibility
 */
export const useTourInteractions = useTourInteractionsImpl;
