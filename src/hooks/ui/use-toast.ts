
// This file should be moved from src/components/ui/use-toast.ts to here if possible
// Since it's a read-only file, we're creating a wrapper to maintain the correct imports
import { useToast, toast } from "@/components/ui/use-toast";

export { useToast, toast };
