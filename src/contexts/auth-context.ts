
/**
 * Re-export from the auth feature folder to maintain backward compatibility
 * This allows existing components to keep working while we transition to the new structure
 */
export * from '@/features/auth/contexts/auth-context';
