
import React from "react";

/**
 * TourAnalyticsTracker Component
 * 
 * Tracks tour analytics events like:
 * - Tour started
 * - Step viewed
 * - Tour completed
 * - Tour abandoned
 */
export const TourAnalyticsTracker: React.FC = () => {
  // This component doesn't render anything visible
  // It just tracks analytics through hooks and effects
  return null;
};
