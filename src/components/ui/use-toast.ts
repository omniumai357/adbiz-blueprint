
// This file is now just a direct export from the hooks file
// to maintain backward compatibility for existing imports
import { useToast, toast } from "@/hooks/use-toast";

export { useToast, toast };
